# This is the transformation catalog. It lists information about each of the
# executables that are used by the workflow.

tr wget {
    site local {
        pfn "/bin/wget"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "/bin/wget"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr gzip {
    site local {
        pfn "/bin/gunzip"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "/bin/gunzip"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr concat {
    site local {
        pfn "${PWD}/bin/concatenate.sh"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/concatenate.sh"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr concat-bpe {
    site local {
        pfn "${PWD}/bin/concat-bpe.sh"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/concat-bpe.sh"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}


tr learnbpe {
    site local {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr applybpe {
    site local {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr getvocab {
    site local {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/fastBPE/fast"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}


tr binarize {
    site local {
        pfn "${PWD}/bin/preprocess.py"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/preprocess.py"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr fasttext {
    site local {
        pfn "${PWD}/bin/fastText/fasttext"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/fastText/fasttext"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}

tr training {
    site local {
        pfn "${PWD}/bin/main.py"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }

    site condorpool {
        pfn "${PWD}/bin/main.py"
        arch "x86_64"
        os "LINUX"
        type "INSTALLED"
    }
}


#cont moses {
#    # can be either docker or singularity
#    type "docker"
#
#    # URL to image in a docker|singularity hub OR
#    image "docker:///lpottier/moses:latest"
#
#    #image_site "pegasus"
#}

